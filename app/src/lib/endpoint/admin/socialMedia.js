const BASE_URL = '/api/admin/v1';

const socialMediaEndpoints = {
  // Get social media information
  getSocialMedia: `${BASE_URL}/social-media`,
  
  // Update social media information
  updateSocialMedia: `${BASE_URL}/social-media`,
};

export default socialMediaEndpoints; 